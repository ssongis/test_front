import { CommonWrapper } from '@/styles/style';
import React from 'react';

const Comment = props => {
    return (
      <CommonWrapper>
        <div>
            댓글
        </div>
      </CommonWrapper> 
      );
  }

  export default Comment;